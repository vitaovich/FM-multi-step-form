class Addon {
    id: string
    name: string
    price: number

    constructor(name: string, price: number) {
        this.id = name
        this.name = name
        this.price = price
    }
}

export default Addon