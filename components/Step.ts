class Step {
    id: number
    num: number
    title: string
    description: string

    constructor(num: number, title: string, description: string) {
        this.id = num
        this.num = num
        this.title = title
        this.description = description
    }
}

export default Step